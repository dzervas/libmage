[package]
name = "mage"
version = "0.1.0"
authors = ["Dimitris Zervas <dzervas@dzervas.gr>"]

[lib]
name = "mage"
crate-type = ["lib", "cdylib", "staticlib"]
path = "src/lib.rs"

[dependencies]
custom_error = "*"
sodiumoxide = "*"

[build-dependencies]
cbindgen = "*"

[features]
default = [ "ffi" ]

ffi = []
